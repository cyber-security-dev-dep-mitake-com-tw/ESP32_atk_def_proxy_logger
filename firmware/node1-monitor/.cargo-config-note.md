Each node needs a .cargo/config.toml selecting the xtensa target
(target = "xtensa-esp32-espidf") and ESP-IDF env, generated by
`cargo generate esp-rs/esp-idf-template`. Omitted here to keep the repo
toolchain-agnostic; see firmware/README.md.
